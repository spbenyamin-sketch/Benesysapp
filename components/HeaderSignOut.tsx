// Online mode puts Sign out in the corner of every page (see the .web version).
// On the phone it lives in Settings, where the header is already carrying the
// screen's own actions and a stray Sign out beside Edit would be a misfire
// waiting to happen.
export default function HeaderSignOut() {
  return null;
}
